#include <vector>
#include <string>
void generate_bill(std::vector<std::string> &person_detail, std::vector<std::string> &service_detail);
void generate_bill(std::vector<std::string> &person_detail, std::vector<std::string> &service_detail,int time);
